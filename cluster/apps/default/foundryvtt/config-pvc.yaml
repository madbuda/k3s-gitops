---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: foundryvtt-config
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
